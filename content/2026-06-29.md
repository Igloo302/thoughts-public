## 今日事项

### 🤖 AI 助手
- 确认本地 `bibi` 的 Hermes 进程仍在运行，对应 `PID 1453`，自 `21Jun26` 起持续运行
- 兴趣雷达快照完成自动更新：兴趣标签 `9 → 10`、关键词 `41 → 50`、项目 `0 → 3`
- 兴趣列表启动衰减，7 个已超过 30 天未匹配的标签被降为 Low，智能硬件/IoT 因过期被移除
- 执行每日兴趣监测 cron：共扫描 `61` 条内容，产出 `3` 条 watch 项，无 top 项
- 完成 `brew install mole` 安装
