## 今日事项

### 🤖 AI 助手
- 修复 Hermes 周备份 Cronjob（`backup-hermes.sh`）大文件提交失败 #66，处理脚本超时问题
- 排查 OpenRouter 免费模型自动切换 Cronjob（`5022f2450835`）的 `Model parameter is required` 报错
- 重构 interest-radar skill：移除种子兴趣回退路径，快照过期时改用旧缓存降级，更新 SKILL.md 与示例文档
- 切换主要对话模型为 DeepSeek Chat（telegram 平台）
- 手动触发每日兴趣监测 Cron（`b0a78b9dc88f`），验证更新后的 skill 调用链
- 完成 Cubox 每日收录任务
