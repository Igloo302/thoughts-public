## 今日事项

### 🤖 AI 助手
- **3DGS 训练环境搭建（Windows VM + RTX 5070）**：解决 Postshot 免费版无法导出的问题，建立自训练管线
- **gsplat 从源码编译（sm_120 支持）**：预编译 wheel 无 Blackwell kernel，在 RTX 5070 上跑光栅化会触发 TDR；通过 `pip install --config-settings=cmake.args="-DCMAKE_CUDA_ARCHITECTURES=120"` 编译成功，验证 `GaussianRasterizer()` 可正常运行
- **COLMAP 稀疏重建完成**：从视频 `IMG_6527.MOV` 抽帧 24 张（2fps），跑通特征提取→匹配→稀疏重建，23/24 帧成功注册，模型输出至 `C:\Users\igloo\3dgs_work\sparse\0\`
- **训练数据就绪**：图片在 `3dgs_work\images\`，稀疏模型在 `3dgs_work\sparse\0\`，下一步需装 torch CUDA 2.11+cu128 后跑 gsplat 训练（30k iter，默认稠密化/剪枝策略）并导出 `.ply/.splat`
- **torch CUDA 安装受阻**：2.7GB wheel 下载超时（公网不稳），已切换 PowerShell `Invoke-WebRequest` 断点续传方案，明天继续