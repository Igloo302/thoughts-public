# 2026-05-24 日记

## 今日事项

### 🤖 AI 助手
- **Hermes 备份到 GitHub**：完成整个 Hermes 配置的 GitHub 备份
  - 远程仓库：`https://github.com/Igloo302/hermes.git`
  - 备份内容：config.yaml、skills/、scripts/、cron/、profiles/、memories/、.gitignore
  - 排除：.env（密钥）、venv、node_modules、checkpoints、logs、caches、lsp、weixin
  - 仓库从 646MB 清理到 76MB（重新初始化 git 仓库，清除历史垃圾）
  - 认证方式：gh CLI token（HTTPS）
- **设置每周自动备份 cronjob**：创建 `~/.hermes/scripts/backup-hermes.sh`，只在有变更时 commit 和 push，无变更静默退出
- **修复 Hermes WebUI**：8787 端口之前退出，重新启动了 WebUI 服务（`python3 bootstrap.py --host 0.0.0.0 8787`）
- **内存清理**：清理过时记忆（interest-radar 的 scoring-calibration 旧版信息），腾出空间

### ⚠️ 问题记录
- **venv 损坏**：多个定时 cronjob（网络通道监控）因 `~/.hermes/hermes-agent/venv/bin/python` 不存在而失败
  - 根本原因：hermes-agent 的虚拟环境丢失或损坏
  - 影响：terminal 和 execute_code 工具均无法使用
  - 建议修复：`cd ~/.hermes/hermes-agent && python3 -m venv venv && pip install -r requirements.txt`