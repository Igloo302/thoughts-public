# 2026-05-25 周一

## 今日事项

### 🤖 AI 助手/Hermes

- **Hermes GitHub 备份**：完成 `~/.hermes` 完整备份推送到 GitHub（`Igloo302/hermes.git`），创建备份脚本 `backup-hermes.sh`，排除 .env/venv/cache 等运行时文件
- **设置每周自动备份 cronjob**：配置 `backup-hermes` 每周日 22:00 自动执行 git add + commit + push，无变更时静默退出
- **启动 Hermes WebUI**：8787 端口 WebUI 恢复运行

### 🛠 QMD 修复与配置

- **qmd 嵌入模型丢失修复**：`Qwen3-Embedding-0.6B-Q8_0.gguf` 文件丢失导致 Node.js 崩溃，强制启用 CPU 模式（`QMD_FORCE_CPU=1`）规避 Metal GPU 崩溃问题
- **MCP 配置修复**：qmd HTTP SSE MCP 与 Hermes 内置 MCP Client 存在协议兼容性问题（status 可用但 query/get 不可用），改为 stdio 模式 (`command: qmd mcp`) 修复，设 timeout 120s
- **性能基准**（CPU 模式）：vsearch ~1.5s，query ~55s（903 篇 Obsidian 笔记）—— 日常推荐用 vsearch

### 📝 Quartz 博客修复

- **修复 PARA 目录暴露问题**：Quartz 博客将 Obsidian vault 中 `1-Projects/English for XR/` 等项目目录同步到公开博客，在 `quartz.config.ts` 的 `ignorePatterns` 加上 `1-Projects/2-Areas/3-Resources/4-Archive/Clippings/Diary/Inbox/0-Inbox` 目录
- **修复 esbuild 依赖缺失**：`@esbuild/darwin-arm64` 包缺失导致 build 失败，`npm install` 后重构成功（116 个 markdown → 297 个输出文件），推送至 GitHub

### 🖥 Windows 台式机管理

- **尝试关闭 Windows VM**（192.168.5.109）：ZeroTier 网络可达但 SSH 认证失败，VM 可能已关机
- **发现 192.168.5.139**：另一台 Windows 设备，SSH 22 端口开放但无可用凭据

### 🌐 其他

- **LLM Wiki 评估**：研究将现有 Obsidian vault 与 LLM Wiki 三层架构（raw/、页面、SCHEMA.md）结合的方案
- **MCP 配置查询**：确认当前 Hermes 的 MCP server 配置状态